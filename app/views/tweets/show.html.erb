<h1><%= @tweets.handle %></h1>

<p> Content: <%= @tweets.content %> </p>

<%= link_to "Back to the Tweets!", tweets_path %>

<%= link_to "Delete this Tweet!", tweet_path(@tweets), method: :delete, data: { confirm: "Are you sure you want to delete this Tweet?"} %>
